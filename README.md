# STM32-Digit-Recognition
The commonly used character recognition model was created through the python programming language. The model has been converted to a format suitable for the STM32 environment and loaded into the STM32F4. The model was run on STM32F4.
